# OCR package | Package OCR

Future optional local-only OCR boundary. Cloud OCR must never become an implicit default.

Futuro confine OCR opzionale solo locale. L'OCR cloud non deve mai diventare un default implicito.
