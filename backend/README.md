# Optional backend | Backend opzionale

There is no backend in the MVP. This reserved boundary exists only to prevent an accidental server dependency from being introduced into local-first flows.

Non esiste un backend nell'MVP. Questo confine riservato serve solo a impedire che venga introdotta accidentalmente una dipendenza server nei flussi local-first.
