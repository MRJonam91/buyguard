# Tests | Test

This directory will host cross-package integration and end-to-end tests. Unit tests remain colocated with their packages when tooling is introduced.

Questa directory ospitera test di integrazione cross-package e end-to-end. Gli unit test resteranno collocati nei rispettivi package quando verra introdotta la toolchain.
