# Browser extension | Estensione browser

Reserved for the Manifest V3 shell, content scripts, side panel and browser-storage boundary. M1 will add the first runnable extension after the canonical contracts and fixture loader exist.

Riservata alla shell Manifest V3, content script, side panel e confine browser storage. M1 aggiungera la prima estensione eseguibile dopo la creazione dei contratti canonici e del fixture loader.
