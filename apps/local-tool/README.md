# Local tool | Strumento locale

Reserved for manual URL, HTML, screenshot and text analysis plus local JSON/Markdown export. It consumes the shared core and does not introduce a mandatory server.

Riservata all'analisi manuale di URL, HTML, screenshot e testo piu export JSON/Markdown locale. Consuma il core condiviso e non introduce un server obbligatorio.
