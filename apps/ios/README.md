# iOS future companion | Companion iOS futuro

Future-only area. Any implementation must rely on officially supported iOS extension, share or shortcut capabilities and reuse the shared analysis core where feasible.

Area solo futura. Ogni implementazione deve basarsi su capacita ufficialmente supportate da estensioni, share o shortcut iOS e riusare il core di analisi condiviso quando possibile.
