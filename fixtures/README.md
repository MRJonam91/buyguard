# Fixtures | Fixture

Store sanitized and non-personal test listings here, organized by detector and outcome: `positive`, `negative`, `borderline`. Fixtures must state their source type and preserve no credentials or personal data.

Conservare qui fixture di annunci sanificate e non personali, organizzate per detector e risultato: `positive`, `negative`, `borderline`. Le fixture devono indicare il tipo di fonte e non conservare credenziali o dati personali.
